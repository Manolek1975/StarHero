<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.StarHero" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Status bar color. -->
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
        <item name="android:windowTranslucentStatus">true</item>
        <!-- Customize your light theme here. -->
        <item name="android:textColor">@color/primary</item>
        <item name="android:fontFamily">@font/aclonica</item>
    </style>

    <style name="Theme.StarHero" parent="Base.Theme.StarHero" />

    <style name="Dot" parent="Base.Theme.StarHero">
        <item name="android:background">@drawable/layout_dot</item>
        <item name="android:layout_gravity">center</item>">
        <item name="android:gravity">center</item>">
        <item name="android:layout_width">wrap_content</item>"
        <item name="android:layout_height">wrap_content</item>"
    </style>

</resources>